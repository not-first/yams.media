[project]
name = "yams-media"
version = "0.1.0"
description = "Material for MKDocs experimental documentation site for YAMS (Yet Another Media Server)"
requires-python = ">=3.14"
dependencies = [
    "mkdocs-git-revision-date-localized-plugin>=1.5.1",
    "mkdocs-material>=9.7.1",
]
